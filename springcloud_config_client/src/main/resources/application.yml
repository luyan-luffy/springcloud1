
server:
  port: 3333
spring:
  rabbitmq:
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    host: localhost
  application:
    name: config-cloud-client
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        serviceId: config-cloud-server
      label: testcloud1

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  cloudfoundry:
    enabled: false
